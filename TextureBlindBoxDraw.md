# Skill: 纹理顺势盲盒创意绘画 TextureBlindBoxDraw_v1.3.1

## 安装

```bash
npx skills add https://github.com/fusuzhenxin/skill-TextureBlindBoxDraw --skill texture-blind-box-draw
```

只安装到 Codex 并全局使用：

```bash
npx skills add https://github.com/fusuzhenxin/skill-TextureBlindBoxDraw --skill texture-blind-box-draw --agent codex --global --yes
```

Agent 实际读取的入口是：

```text
skills/texture-blind-box-draw/SKILL.md
```

## v1.3.1 定位

生成图必须与原图有一眼可见的创意差异，同时保持原照片仍然是画面基底。

- 原图实拍信息保留至少70%，未规划区域不得重绘。
- 默认强度3、可见度4、惊喜度4。
- 512px预览中，1秒内看出变化区域，约3秒内看懂事件或笑点。
- 不放大图片也必须看清主角轮廓、动作和关键道具，不能只看到一团线。
- 主事件在1024px图中最长边约180-320px，主线约5-8px。
- 实际涂鸦线条保持稀疏，但原图物体共同构成的事件视觉范围约占15%-30%。
- 黑色马克笔线条为主；复杂背景可使用极窄白色分隔边，或在单个小道具上使用一种取自原图的强调色。

“隐藏”指第二层含义值得发现，不代表把主事件画到几乎看不见。

## 可读性优先

- 干净背景：主角高度约为画面短边的7%-11%。
- 普通背景：主角高度约为画面短边的10%-15%。
- 复杂或高对比背景：主角高度约为画面短边的14%-20%。
- 配角约为主角的60%-80%；低于短边6%的角色只能作为彩蛋。
- 大型真实物体已经承担事件主体时，小人可以更小，但512px下仍须看懂动作。
- 看不清时依次放大主角、减少配角、简化线条、增强分隔或换到干净位置，不要增加长梯子、长绳和箭头撑大事件范围。

## 创意公式

每次创作按以下顺序构思：

1. 从原图找出3-5个可用特征。
2. 选择两处相互关联的真实特征。
3. 把它们共同误读成角色、装置、事件原因或事件结果。
4. 加入一个明确动作及其结果、失败、误会或反转。
5. 用接触、遮挡、透视、受力、风向或水流把涂鸦锁在照片表面。
6. 设置一个主要视觉区和1-2个较小呼应线索。

目标不是“照片上出现几个小人”，而是“观众突然发现真实纹理一直在扮演另一件东西”。

## 默认淘汰项

出现以下情况时，不应直接交付，应重新构思：

- 只有几个小眼睛或笑脸。
- 孤立火柴人站在物体上。
- 一条箭头、直线或长绳横跨画面。
- 通用攀爬、测绘、拉绳、插旗场景。
- 涂鸦小到缩略图中看不见。
- 创意可以原样搬到任意一张照片。
- 为了制造差异而把整张照片重绘成插画。

## 惊喜机制

优先使用：

- 错认：两处真实特征组合成一个新的身份。
- 因果：涂鸦动作解释原图中已经存在的水波、污渍、碎屑或倾斜。
- 失败任务：角色认真完成一件被现实尺度注定破坏的工作。
- 缺席痕迹：不画主角，只保留工具、脚印和事件结果。
- 尺度反转：日常小物成为角色眼中的重大危机。
- 角色反转：现实物体反过来管理人类留下的痕迹。
- 合作系统：两个真实物体分别成为事件的输入与输出。

## 五种模式

- `CrackTrace`：裂缝、划痕和接触的第二处特征共同形成事件结构。
- `ObjectAnthropo`：物体承担角色身体或巨型道具，不只增加五官。
- `MicroWorld`：角色真实接触照片表面，主事件小而不弱。
- `SymbolDecorate`：不画生命体时，用纹理和符号形成可读隐喻。
- `CustomFree`：落实用户指定创意，同时执行保真和惊喜检查。

## 输出字段

- `active_mode`
- `intensity_level`：默认3
- `visibility_level`：默认4
- `surprise_level`：默认4
- `source_clues`：至少两处原图特征
- `hidden_event`
- `visual_punchline`
- `texture_evidence`
- `character_relation`
- `reveal_point`
- `difference_note`
- `readability_note`
- `quality_score`
- `prompt_set`
- `suggest_user_msg`
- `forbidden_trigger`

## 质量门槛

按每项0-2分评分，总分至少8/10且不得出现0分：

- 原图保留
- 差异明显
- 纹理依赖
- 物理融合
- 创意惊喜

详细执行规则见 `skills/texture-blind-box-draw/SKILL.md`。
